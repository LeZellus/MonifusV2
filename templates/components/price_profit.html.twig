{%- if lot is defined -%}
{{ profit(lot, mode|default('compact')) }}
{%- elseif sale is defined -%}
{{ sale_profit(sale, mode|default('compact')) }}
{%- else -%}
{{ manual_profit(buy_price|default(0), sell_price|default(0), quantity|default(1), mode|default('compact')) }}
{%- endif -%}