{% extends 'base.html.twig' %}

{% block title %}Sauvegarde des Données{% endblock %}

{% block body %}
    <div class="wrapper-container">
        <div class="max-w-4xl mx-auto grid gap-8">
            <!-- Résumé des données -->
            <div class="grid grid-cols-2 md:grid-cols-5 gap-4">
                {% include 'components/stats_card.html.twig' with {
                    title: 'Profils',
                    value: summary.profiles_count,
                    color: 'blue'
                } %}
                {% include 'components/stats_card.html.twig' with {
                    title: 'Personnages',
                    value: summary.characters_count,
                    color: 'green'
                } %}
                {% include 'components/stats_card.html.twig' with {
                    title: 'Lots',
                    value: summary.lots_count,
                    color: 'purple'
                } %}
                {% include 'components/stats_card.html.twig' with {
                    title: 'Ventes',
                    value: summary.sales_count,
                    color: 'yellow'
                } %}
                {% include 'components/stats_card.html.twig' with {
                    title: 'Surveillances',
                    value: summary.watches_count,
                    color: 'orange'
                } %}
            </div>

            <!-- Actions de sauvegarde -->
             <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <!-- Export des données -->
                <div class="wrapper-background">
                    <div class="flex items-center mb-4">
                        <span class="text-2xl mr-3">💾</span>
                        <h3 class="text-white text-lg font-semibold">Exporter mes données</h3>
                    </div>
                    <p class="text-gray-300 mb-4">
                        Téléchargez toutes vos données de trading au format JSON. 
                        Ce fichier contient vos profils, personnages, lots, ventes et surveillances.
                    </p>
                    <a href="{{ path('app_backup_export') }}" 
                       class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-lg transition-colors inline-block">
                        📥 Télécharger la sauvegarde
                    </a>
                </div>

                <!-- Import des données -->
                <div class="wrapper-background">
                    <div class="flex items-center mb-4">
                        <span class="text-2xl mr-3">📤</span>
                        <h3 class="text-white text-lg font-semibold">Importer mes données</h3>
                    </div>
                    <p class="text-gray-300 mb-4">
                        Restaurez vos données depuis un fichier de sauvegarde JSON.
                        Idéal pour migrer ou récupérer vos données.
                    </p>
                    <a href="{{ path('app_backup_import') }}" 
                       class="bg-green-600 hover:bg-green-700 text-white px-6 py-3 rounded-lg transition-colors inline-block">
                        📤 Importer des données
                    </a>
                </div>
            </div>
            
            <!-- Conseils de sauvegarde -->
            {% set backup_tips_content %}
                <p>• Exportez vos données régulièrement (1 fois par mois)</p>
                <p>• Conservez les fichiers de sauvegarde en lieu sûr</p>
                <p>• Vérifiez que l'export contient bien toutes vos données</p>
                <p>• En cas de problème, contactez le support avec votre sauvegarde</p>
            {% endset %}

            {% include 'components/info_box.html.twig' with {
                title: 'Conseils de sauvegarde',
                icon: '💡',
                color: 'green',
                content: backup_tips_content
            } %}
        </div>
    </div>
{% endblock %}