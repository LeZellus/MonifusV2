<?php
// 1. === MÉTHODE 1: Template de test sans Turbo ===
// templates/test/performance.html.twig

?>
<!DOCTYPE html>
<html>
<head>
    <title>Test Performance - Sans Turbo</title>
    <!-- Pas de turbo ici ! -->
</head>
<body>
    <h1>Test Performance</h1>
    <p><strong>Temps d'exécution:</strong> {{ executionTime }} ms</p>
    <p><strong>Nombre de requêtes:</strong> {{ totalQueries }}</p>
    
    <h2>Détail des requêtes:</h2>
    {% for query in queries %}
        <div style="margin-bottom: 20px; padding: 10px; background: #f5f5f5;">
            <strong>Requête {{ loop.index }}:</strong><br>
            <pre>{{ query.sql }}</pre>
            <small>Temps: {{ query.executionMS }}ms</small>
        </div>
    {% endfor %}
    
    <hr>
    <a href="{{ path('test_performance') }}">Relancer le test</a>
</body>
</html>